# Key binds
# clear_all_shortcuts yes
map alt+shift+r load_config_file

## Layout
map alt+l next_layout

## Tabs
map cmd+1 goto_tab 1
map cmd+2 goto_tab 2
map cmd+3 goto_tab 3
map cmd+4 goto_tab 4
map cmd+5 goto_tab 5
map cmd+6 goto_tab 6
map cmd+7 goto_tab 7
map cmd+8 goto_tab 8
map cmd+9 goto_tab 9

# Tab mode
map --new-mode tab cmd+shift+t
map --mode tab esc pop_keyboard_mode
map --mode tab enter pop_keyboard_mode
map --mode tab cmd+shift+t pop_keyboard_mode
map --mode tab cmd+shift+p combine : pop_keyboard_mode : push_keyboard_mode pane
map --mode tab cmd+shift+w combine : pop_keyboard_mode : push_keyboard_mode zmx
map --mode tab cmd+shift+r combine : pop_keyboard_mode : push_keyboard_mode resize
map --mode tab r combine : pop_keyboard_mode : set_tab_title
map --mode tab x combine : pop_keyboard_mode : close_tab
map --mode tab t combine : pop_keyboard_mode : new_tab
map --mode tab right next_tab
map --mode tab left previous_tab
map --mode tab shift+right move_tab_forward
map --mode tab shift+left move_tab_backward

## Copy/Paste
map cmd+c copy_to_clipboard
map cmd+v paste_from_clipboard
map ctrl+shift+c copy_to_clipboard
map ctrl+shift+v paste_from_clipboard

## Panes
# Pane mode
map --new-mode pane cmd+shift+p
map --mode pane esc pop_keyboard_mode
map --mode pane enter pop_keyboard_mode
map --mode pane cmd+shift+p pop_keyboard_mode
map --mode pane cmd+shift+t combine : pop_keyboard_mode : push_keyboard_mode tab
map --mode pane cmd+shift+w combine : pop_keyboard_mode : push_keyboard_mode zmx
map --mode pane cmd+shift+r combine : pop_keyboard_mode : push_keyboard_mode resize
map --mode pane r combine : pop_keyboard_mode : set_window_title
map --mode pane x combine : pop_keyboard_mode : close_window
map --mode pane h combine : pop_keyboard_mode : kitten kittens/smart_split.py hsplit
map --mode pane v combine : pop_keyboard_mode : kitten kittens/smart_split.py vsplit
map --mode pane shift+h combine : pop_keyboard_mode : launch --location=hsplit
map --mode pane shift+v combine : pop_keyboard_mode : launch --location=vsplit
map --mode pane right move_window right
map --mode pane left move_window left
map --mode pane up move_window up
map --mode pane down move_window down

### Move between panes, even when in editor
map shift+left neighboring_window left
map shift+right neighboring_window right
map shift+up neighboring_window up
map shift+down neighboring_window down
map --when-focus-on title:hx.* shift+left neighboring_window left
map --when-focus-on title:hx.* shift+right neighboring_window right
map --when-focus-on title:hx.* shift+up neighboring_window up
map --when-focus-on title:hx.* shift+down neighboring_window down

### Resizing
# Resize mode
map --new-mode resize cmd+shift+r
map --mode resize esc pop_keyboard_mode
map --mode resize enter pop_keyboard_mode
map --mode resize cmd+shift+r pop_keyboard_mode
map --mode resize cmd+shift+t combine : pop_keyboard_mode : push_keyboard_mode tab
map --mode resize cmd+shift+p combine : pop_keyboard_mode : push_keyboard_mode pane
map --mode resize cmd+shift+w combine : pop_keyboard_mode : push_keyboard_mode zmx
map --mode resize left resize_window narrower
map --mode resize right resize_window wider
map --mode resize up resize_window taller
map --mode resize down resize_window shorter

## Remote zmx sessions
# Zmx mode
map --new-mode zmx cmd+shift+w
map --mode zmx esc pop_keyboard_mode
map --mode zmx enter pop_keyboard_mode
map --mode zmx cmd+shift+w pop_keyboard_mode
map --mode zmx cmd+shift+t combine : pop_keyboard_mode : push_keyboard_mode tab
map --mode zmx cmd+shift+p combine : pop_keyboard_mode : push_keyboard_mode pane
map --mode zmx cmd+shift+r combine : pop_keyboard_mode : push_keyboard_mode resize
# Close a zmx pane → detach that session (it keeps running remotely).
# exit / Ctrl-D  → kill that session.
# Resume a prior session.
map --mode zmx r combine : pop_keyboard_mode : send_text all \x15zx\r
# Detach the focused zmx session only. The fallback exits the mode without
# touching ordinary local and non-zmx SSH panes.
map --mode zmx d pop_keyboard_mode
map --mode zmx --when-focus-on var:zmx_session d combine : pop_keyboard_mode : close_window
# Detach all: drop the SSH ControlMaster (all sessions persist remotely).
map --mode zmx shift+d combine : pop_keyboard_mode : launch --type=overlay zsh -ic zd
# Layout save (overlay — needs real window context for kitten @) / restore (hsplit)
map --mode zmx s combine : pop_keyboard_mode : launch --type=overlay zsh -ic zls
map --mode zmx o combine : pop_keyboard_mode : launch --type=overlay zsh -ic zlr
